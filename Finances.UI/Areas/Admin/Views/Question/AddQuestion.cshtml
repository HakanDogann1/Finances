@model Finances.DtoLayer.DTOs.Question.AddQuestionDto
@{
    ViewData["Title"] = "AddQuestion";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<div class="card">
    <div class="card-body wizard-content">
        <h4 class="card-title">Soru Ekleme Sayfası</h4>
        <h6 class="card-subtitle"></h6>
        <form method="post">
            <div>
                <section>
                    <label for="userName">Başlık </label>
                    <input type="text" class="required form-control" asp-for="QuestionTitle">
                    <label for="password">İçerik</label>
                    <input type="text" class="required form-control" asp-for="QuestionDescription">
                </section>
                <br />
                <button class="btn btn-outline-success">Ekle</button>
            </div>
        </form>
    </div>
</div>

